# 球面轨迹覆盖显示：当前实现说明

本文只记录当前代码中实际生效的方案。此前使用球面贴图（将轨迹画入一张图片后映射到球面）的尝试已经删除，不再是运行路径的一部分。

## 1. 目标与结论

球面覆盖视图现在使用**真实三维轨迹带**：每段原始轨迹在球面切平面内展开为一个很窄的四边形，并拆成两个三角形；所有三角形合并到一个 `Poly_Triangulation`，以一个 `AIS_Triangulation` 对象显示。

这意味着：

- 不依赖 UV 展开、贴图分辨率或纹理过滤，因此局部覆盖、球顶和全部覆盖不会产生贴图拉伸/模糊；
- 轨迹带参与 OCC 的正常深度测试，可以由凹球面的实体外壳遮挡；
- 不再为每一个轨迹采样点生成一个 AIS 顶点对象；
- 覆盖轨迹颜色为纯亮蓝 `RGB(0, 0, 255)`，与分开展示的轨迹颜色一致；
- 凹球面从外侧观察时，轨迹不会穿出实体外壳。

主要实现位于 `main.py`：

- 轨迹带网格：`PreviewCanvas._display_spherical_direct_overlay()`；
- 显示间隙：`PreviewCanvas._surface_overlay_points()`；
- 球面实体/壳体：`PreviewCanvas._spherical_surface_parts()`；
- 球面轨迹生成：`function/traj_spherical.py::generate_spherical()`。

## 2. 固定厚度与几何定义

固定材料厚度为：

```python
SPHERICAL_WALL_THICKNESS_MM = 0.5
```

该值定义在 `function/traj_spherical.py`，由 GUI 生成逻辑传入球面轨迹生成器，同时写入显示几何参数。

### 2.1 凸球面

凸球面的加工面是外球面，半径为 `R`。

```text
加工面半径：R
球面中心：    (0, 0, h - R)       （预览坐标系）
```

为了让预览的球冠表现为有厚度的实体，显示几何还包含：

- 半径 `R - t` 的内球面；
- 开口处的环形封口面；
- 外球面作为实际加工面。

轨迹生成点本身仍在半径 `R` 的外球面上。

### 2.2 凹球面

凹球面的加工面是外球壳的内表面，因此轨迹生成和显示使用同一工作半径：

```text
外球壳半径：R
加工内球半径：R - t
球面中心：    (0, 0, R)           （预览坐标系）
```

其中 `t = 0.5 mm`。凹球面显示实体由以下部分组成：

- 外球面；
- 开口处的环形封口面；
- 半径 `R - t` 的内球加工面。

凹球面轨迹点的法向指向腔体/球心方向。也就是说，轨迹点确实生成在内球面上，而不是把外球面上的轨迹简单向内平移。

## 3. 轨迹生成

球面轨迹输出格式为：

```text
[x, y, z, nx, ny, nz]
```

前 3 项是预览坐标，后 3 项是加工法向。栅形轨迹按球面弧长参数化：

- 行间距由 `R * Δu` 控制；
- 行内步长由 `R * cos(u) * Δβ` 控制；
- 因此输入的步长和间距对应球面上的实际弧长，而不是简单的 XY 投影距离。

局部矩形/圆形覆盖采用“先生成完整球冠轨迹，再按覆盖区域筛选”的方式。因此局部轨迹与全部覆盖轨迹保持相同的栅格相位，不会因为局部区域重新起栅而错位。

螺旋轨迹也先按完整投影圆生成，再做局部区域筛选。对于螺旋的非连续跨越段，显示阶段会利用长度阈值断开，避免错误地用一条长直线连接。

## 4. 覆盖显示的轨迹带

### 4.1 不再使用贴图

已删除的贴图方案曾有以下结构：

```text
轨迹点 -> 球面 UV 展开 -> PNG 图片 -> AIS_TexturedShape
```

它的问题是球顶附近或局部区域会发生纹理压缩，导致轨迹变糊、形成旋涡状聚集，且轨迹颜色会受纹理过滤影响。

当前路径没有 `AIS_TexturedShape`、临时 PNG、`tempfile` 或纹理映射逻辑。

### 4.2 三角形轨迹带的构造

对每个连续轨迹段 `p1 -> p2`：

1. 取线段单位切向 `T = normalize(p2 - p1)`；
2. 读取两端原始加工法向 `N1`、`N2`；
3. 在球面切平面内计算侧向：

   ```text
   S1 = normalize(N1 × T)
   S2 = normalize(N2 × T)
   ```

4. 若 `S1` 和 `S2` 方向相反，则翻转 `S2`，避免轨迹带扭转；
5. 生成四个顶点：

   ```text
   p1 + w*S1,  p1 - w*S1,
   p2 + w*S2,  p2 - w*S2
   ```

6. 用两个三角形组成该段四边形带。

轨迹带半宽为：

```python
max(0.0015 * 当前轨迹包围盒最大尺寸, 0.18 mm)
```

此宽度只影响显示效果，不改变导出的轨迹坐标或加工路径。

所有轨迹带片合并为一个 `Poly_Triangulation`，再以一个 `AIS_Triangulation` 显示。因而即使全部覆盖有约三万段轨迹，也不会创建三万个 AIS 显示对象或三万个点对象。

### 4.3 蓝色与双面显示

轨迹带的着色器颜色直接设为：

```text
RGB(0, 0, 255)
```

并允许显示三角形的背面，避免因球面内外方向不同而使轨迹带消失。由于颜色来自三维显示对象而不是曲面纹理，颜色不会被球面贴图的光照调制成灰蓝色。

## 5. 深度冲突、显示间隙与凹球面遮挡

如果轨迹带和球面完全共面，深度缓冲会发生 z-fighting：某些像素显示球面、某些像素显示轨迹，结果表现为断续、闪烁或局部消失。

为避免该问题，覆盖视图只使用一个**显示坐标副本**：

```text
p_display = p_original + clearance * normal
```

原始 `points` 不会被改写，因此：

- 导出 TXT 的坐标不变；
- 分开展示的轨迹数据不变；
- 只有覆盖预览中的轨迹带使用显示间隙。

### 5.1 凸球面间隙

凸球面沿外法向抬高，确保轨迹带位于加工面之前而清晰可见。间隙由球半径和厚度共同限制：

```text
clearance = min(max(0.002 * R, 0.02), max(0.40 * t, 0.02))
```

### 5.2 凹球面间隙

凹球面的法向朝腔体/球心，显示间隙同样沿该方向移动。凹球面额外使用更大的安全间隙，最大约为 `0.90 * t`，使轨迹带稳定地位于内腔一侧，而外球壳仍位于其前方。

效果是：

- 从开口/腔体一侧看，轨迹带在内球加工面上方清晰可见；
- 从实体外侧看，外球壳和封口环会通过普通深度测试遮挡轨迹带；
- 不需要相机事件、逐帧射线裁剪或强制隐藏轨迹。

## 6. 球面实体边界处理

球面实体使用解析球面面和开口环形面构造，而不是原先粗网格采样的球面三角片。这样可减少可见的分片边界。

显示球面时会关闭：

- 面边界；
- 线框；
- 着色边缘。

并强制重新显示该对象，避免沿母线出现浅色竖直接缝。

## 7. 显示流程

球面覆盖视图的顺序为：

```text
生成原始轨迹点（含法向）
        ↓
构造凹/凸球面实体壳
        ↓
复制轨迹点，并沿法向施加仅用于显示的间隙
        ↓
将连续轨迹段转换为一个三角形轨迹带网格
        ↓
先显示球面实体，再显示亮蓝色轨迹带
        ↓
显示起点/终点标记与坐标轴
```

当三角形带构造失败（例如数据缺少法向）时，程序会自动退回原有的 OCC 边线显示，保证预览不会中断。正常由球面生成器产生的数据均含法向，因此会使用三角形轨迹带路径。

## 8. 已验证结果

临时诊断使用默认密度：

```text
R = 100 mm
h = 100 mm
t = 0.5 mm
步长 = 1 mm
间距 = 2 mm
```

结果如下：

| 场景 | 轨迹段数 | 预览渲染时间 | 蓝色检测结果 |
| --- | ---: | ---: | --- |
| 凸球面局部矩形覆盖 | 4,306 | 约 1.19 s | 主色为 `(0, 0, 255)` |
| 凸球面全部覆盖 | 31,651 | 约 3.02 s | 主色为 `(0, 0, 255)` |
| 凹球面腔内全部覆盖 | 31,334 | 约 2.95 s | 主色为 `(0, 0, 255)` |
| 凹球面实体外侧 | 31,334 | 约 2.85 s | 蓝色像素数为 `0` |

视觉检查结论：

- 凸球面局部覆盖：球顶轨迹清晰，没有贴图压缩模糊；
- 凸球面全部覆盖：轨迹连续、亮蓝色；
- 凹球面腔内：轨迹完整显示在内球面上；
- 凹球面外侧：不显示蓝色轨迹。

诊断脚本、截图以及贴图运行时文件均已删除。语法检查和 `git diff --check` 已通过。

## 9. 范围与维护约束

- 球面覆盖预览使用解析球壳和三维轨迹带；凹柱面覆盖预览也使用同一类深度测试轨迹带和解析柱壳；
- 不改变非球面、锥面或平面轨迹的覆盖逻辑；
- 不新增 GUI 厚度输入项，球面固定使用 `t = 0.5 mm`；
- 不改变轨迹规划、局部覆盖筛选、法向输出或文件导出；
- 轨迹带的宽度和显示间隙只属于可视化层，不应作为加工参数使用；
- 后续若修改球面轨迹的法向定义，必须同时检查 `_surface_overlay_points()` 与三角形带的 `N × T` 侧向计算。

## 10. 凹柱面全部覆盖右侧轨迹透壳：诊断与修复

### 10.1 现象

凹柱面选择“全部覆盖”、默认参数时，左侧和底部的蓝色轨迹正常贴在腔内；但右侧靠近终点的一整片轨迹会显示在灰色柱壳的外侧。视觉上像是轨迹没有覆盖到内表面，或者有一段轨迹穿出了实体。

用于复现的参数为：

```text
R = 100 mm
zc = 0 mm
k_cut = 0 mm
轴线 = Y 轴，范围 [-50, 50] mm
凹柱固定厚度 t = 0.5 mm
全部覆盖，栅形 X 方向步进
步长 = 1 mm，行间距 = 2 mm
```

该组合会生成 `16,014` 个点，和问题截图的点数一致。

### 10.2 先验证轨迹，而不是先改显示

诊断先只显示轨迹，再检查每个点相对柱轴的半径、法向和显示偏移后的半径：

```text
凹柱加工半径（内壁）        99.5 mm = R - t
原始点半径范围               [99.499999397, 99.500000592] mm
最大半径误差                 约 0.0000006 mm
显示偏移后的半径范围         [99.049999149, 99.050000747] mm
显示偏移后的理论半径         99.05 mm = 99.5 - 0.45
相邻路径距离 median / p95 / max
                              约 1.0 / 1.0 / 2.0 mm
```

结论：

- 全部 `16,014` 个原始点都在半径 `R - t` 的凹柱内加工面上；
- 法向都指向腔体/柱轴，显示副本沿法向向腔内移动 `0.45 mm`，没有向外移动；
- 没有异常长连线，最大 `2 mm` 是相邻栅格行之间的合法连接；
- 因此问题不属于轨迹规划、全部覆盖裁剪、法向计算或导出数据，而是纯粹的三维预览遮挡问题。

### 10.3 根因

修复前，凹柱面实体由 `_sample_cylindrical_surface()` 生成的 `36 × 36` 离散三角片拼接而成；凹柱面轨迹又走了一个特殊的普通 OCC 边线分支。

对于厚度只有 `0.5 mm` 的凹柱壳，右侧是从实体外侧可见的半区。离散三角面近似的外壳无法稳定地对腔内轨迹执行正确遮挡，普通边线也不适合作为需要被实体遮挡的覆盖显示对象。结果是物理上位于内壁的轨迹，在右侧视觉上“透过”了外壳。

这解释了为什么凹球面没有同类问题：凹球面已经使用 OCC 的解析内外球面，以及参与深度测试的 `AIS_Triangulation` 轨迹带；而凹柱面当时仍是离散壳体加边线的旧路径。

### 10.4 修复方案

凹柱面现在仿照凹球面的显示策略，修改位置均在 `main.py`：

1. 在 `_sample_cylindrical_surface()` 中，为凹柱面用 `gp_Cylinder` + `BRepBuilderAPI_MakeFace` 创建精确的外柱面和内加工面，并按 `k_cut` 裁出实际可见的柱面弧段。
2. 保留并加密开口两侧的唇边和轴向两端封口，使 `0.5 mm` 厚的预览壳完整闭合。
3. 取消凹柱面的“普通边线”特殊分支，使其和凹球面一样走 `_display_surface_direct_overlay()`：轨迹段展开成 `AIS_Triangulation` 三角形带，参与正常深度测试。
4. 原始轨迹点、轨迹生成公式、局部覆盖筛选、法向输出和 TXT 导出均未改变；只有覆盖预览使用显示坐标副本和新的实体显示方式。

解析柱面可保证右侧外壳在深度缓冲中连续，因而从外侧观察时会遮住腔内蓝色轨迹；从开口/腔体一侧观察时，轨迹仍完整可见。

### 10.5 回归验证

临时诊断分别截图检查了：

- 仅显示默认 `16,014` 点轨迹：完整、连续的半柱面；
- 修复前的离散壳体：右侧出现透壳蓝线；
- 解析柱壳加三维轨迹带：右侧透壳蓝线消失；
- 凹柱轴线平行 X、轴线平行 Y；
- 全部覆盖、局部矩形、局部圆形、非零切割高度；
- 凸柱面和原有凹球面路径。

上述场景均可正常显示。临时诊断脚本、截图和运行时文件已在验证完成后删除。
